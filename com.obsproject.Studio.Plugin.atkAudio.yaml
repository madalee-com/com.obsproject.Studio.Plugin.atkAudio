id: com.obsproject.Studio.Plugin.atkAudio
branch: stable
runtime: com.obsproject.Studio
runtime-version: stable
sdk: org.kde.Sdk//6.8
build-extension: true
separate-locales: false
build-options:
  prefix: /app/plugins/atkAudio
  cflags: -Wno-error
  strip: true

modules:
  - name: atkaudio-plugin
    buildsystem: simple
    build-commands:
      - |
        mkdir packages
        cd _deps/juce-src
        patch -p1 -N --binary < ../../0001-fix-add-flathub-PATHS-to-JUCE.patch
      - sed -i '/include(CTest)/c\# include(CTest)' CMakeLists.txt
      - sed -i '/add_subdirectory(tests)/c\# add_subdirectory(tests)' CMakeLists.txt
      - cmake -S . -B _build -DCMAKE_INSTALL_LIBDIR=lib -DCMAKE_INSTALL_PREFIX="${FLATPAK_DEST}" -DFETCHCONTENT_FULLY_DISCONNECTED=ON --compile-no-warning-as-error
      - cmake --build _build -j4 --config Release
    post-install:
      - install -Dm644 ${FLATPAK_ID}.metainfo.xml -t ${FLATPAK_DEST}/share/metainfo
    sources:
      - type: git
        url: https://github.com/atkAudio/PluginForObsRelease.git
        branch: main
      - type: git
        url: https://github.com/juce-framework/JUCE.git
        tag: 8.0.12
        dest: _deps/juce-src
      - type: file
        path: com.obsproject.Studio.Plugin.atkAudio.metainfo.xml
      - type: file
        path: 0001-fix-add-flathub-PATHS-to-JUCE.patch
